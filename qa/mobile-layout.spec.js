const { test, expect } = require('@playwright/test');
async function noOverflow(page,label=''){const d=await page.evaluate(()=>({scroll:document.documentElement.scrollWidth,client:document.documentElement.clientWidth}));expect(d.scroll,`${label} ${JSON.stringify(d)}`).toBeLessThanOrEqual(d.client+2)}
async function completeIntake(page){for(let i=0;i<3;i++){await noOverflow(page,`intake-${i}`);await page.locator('#fcNext').click()}await noOverflow(page,'intake-generate');await page.locator('#fcGenerate').click();await page.waitForFunction(()=>localStorage.getItem('fitcoach_client_profile_v35')!==null)}
for(const width of [320,375,390,430]) test(`FitCoach fits ${width}px without horizontal overflow`,async({browser})=>{const context=await browser.newContext({viewport:{width,height:820},isMobile:true,hasTouch:true});const page=await context.newPage();await page.goto('http://127.0.0.1:4173/',{waitUntil:'domcontentloaded'});await noOverflow(page,'launch');await completeIntake(page);await noOverflow(page,'home');const buttons=page.locator('nav button');const count=await buttons.count();for(let i=0;i<count;i++){const b=buttons.nth(i);if(await b.isVisible()){await b.click();await page.waitForTimeout(40);await noOverflow(page,`nav-${i}`)}}await context.close()});
test('FitCoach mobile layer includes safe areas and reduced overflow policy',async({page})=>{const js=await (await page.request.get('/mobile-quality-v41.js')).text();expect(js).toContain('overflow-x:hidden');expect(js).toContain('safe-area-inset-bottom');expect(js).toContain('100dvh')});
