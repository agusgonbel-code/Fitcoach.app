import test from 'node:test';import assert from 'node:assert/strict';import {createRequire} from 'node:module';const require=createRequire(import.meta.url);const e=require('../adaptive-review-v36.js');
test('does not change calories with poor adherence',()=>{const r=e.weeklyReview({goal:'loss'},{kcal:2200,protein:160,fat:70},{weights:[80,80,80,80,80,80,80],adherence:.6,recovery:4,sleep:4});assert.equal(r.calorieDelta,0);assert.equal(r.confidence,'medium');});
test('suggests conservative cut when loss trend is too slow',()=>{const r=e.weeklyReview({goal:'loss'},{kcal:2400,protein:170,fat:70},{weights:[80,80,79.9,79.9,79.8,79.8,79.8],adherence:.95,recovery:4,sleep:4});assert.ok(r.calorieDelta<=0);assert.equal(r.nextTargets.protein,170);assert.ok(r.nextTargets.kcal>=1200);});
test('flags deload when performance and recovery collapse',()=>{const r=e.weeklyReview({goal:'recomp'},{kcal:2500,protein:170,fat:75},{weights:[80,79.9,80,79.9,80,79.9,80],adherence:.9,performance:-.8,recovery:1,sleep:2});assert.equal(r.training.deload,true);assert.equal(r.training.volumeModifier,.7);});
test('macro calories remain close after calorie change',()=>{const t=e.applyDelta({kcal:2400,protein:170,fat:70},-100);assert.ok(Math.abs((t.protein*4+t.carbs*4+t.fat*9)-t.kcal)<=4);});