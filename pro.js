(()=>{'use strict';
const $=id=>document.getElementById(id),read=(k,f)=>{try{return JSON.parse(localStorage.getItem(k))??f}catch{return f}},clamp=(n,a,b)=>Math.max(a,Math.min(b,n));
function monday(){const d=new Date(),x=d.getDay()||7;d.setHours(0,0,0,0);d.setDate(d.getDate()-x+1);return d}
function ensure(){if(document.getElementById('fc232'))return;const home=$('home');if(!home)return;const h=document.createElement('h2');h.textContent='Estado 2.3.2';const c=document.createElement('div');c.id='fc232';c.className='card';const anchor=$('homeLast')?.parentElement||home.lastElementChild;home.insertBefore(h,anchor?.nextSibling||null);home.insertBefore(c,h.nextSibling)}
function render(){ensure();const o=$('fc232');if(!o)return;const workouts=read('workouts',[]),settings=read('settings',{weeklySessionGoal:4}),recovery=read('recovery',[]),week=workouts.filter(w=>new Date(w.date)>=monday()),goal=Number(settings.weeklySessionGoal)||4,r=recovery.at(-1);let ready='—';if(r){const sleep=clamp((Number(r.sleep)||0)/8*100,0,100),energy=clamp((Number(r.energy)||3)/5*100,0,100),stress=100-clamp(((Number(r.stress)||3)-1)/4*100,0,100);ready=Math.round(sleep*.4+energy*.35+stress*.25)}o.innerHTML=`<div style="display:flex;justify-content:space-between;gap:12px"><div><strong>Runtime corregido</strong><div class="small">Core FitCoach sin dependencia de CROSS_WODS</div></div><span class="pill">2.3.2</span></div><div class="row" style="margin-top:12px"><div><div class="stat">${week.length}/${goal}</div><div class="label">sesiones semana</div></div><div><div class="stat">${ready}</div><div class="label">readiness</div></div></div>`}
function init(){render();document.addEventListener('click',()=>setTimeout(render,120));window.FitCoachRuntime232={render,version:'2.3.2'}}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init,{once:true}):init()})();
