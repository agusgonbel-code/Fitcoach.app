FITCOACH 2.3.3 — ENTRADA ESTABLE

Sube estos archivos a la raíz del repositorio:
- stable.html
- pro.js
- version.json

No hace falta sustituir app.js ni index.html.

Después abre:
https://agusgonbel-code.github.io/Fitcoach.app/stable.html

Qué cambia:
- stable.html elimina caches y service workers antiguos sin tocar localStorage.
- descarga index.html fresco.
- define CROSS_WODS antes de cargar app.js, por lo que el error de arranque desaparece.
- carga app.js y pro.js directamente, sin parches del service worker.
- no depende de repair.html ni de una cadena de actualizaciones.

Si esta entrada funciona correctamente, el siguiente paso será convertir stable.html en el index.html definitivo.
