(()=>{'use strict';const R=(k,f)=>{try{return JSON.parse(localStorage.getItem(k))??f}catch{return f}},$=id=>document.getElementById(id);function review(){const e=globalThis.FitCoachWeeklyAdaptation;if(!e)return;const result=e.recommend({plan:R('fitcoach_active_plan_v33',{}),workouts:R('workouts',[]).slice(-7),meals:R('meals',[]).slice(-7),metrics:R('metrics',[]).slice(-7),targets:R('targets',{}),profile:R('fitcoach_client_profile_v35',{})});const card=$('coach');if(!card)return;const a=result.adjustments;card.innerHTML=`<strong>Revisión semanal inteligente</strong><div class="muted">Confianza ${result.confidence} · adherencia ${Math.round(result.summary.adherence*100)}%</div><div style="margin-top:8px">${result.reasons.map(x=>`<div>• ${x}</div>`).join('')}</div><div class="muted" style="margin-top:8px">Volumen ${a.volumePercent>0?'+':''}${a.volumePercent}% · carga ${a.loadPercent>0?'+':''}${a.loadPercent}% · energía ${a.kcalDelta>0?'+':''}${a.kcalDelta} kcal${a.deload?' · descarga sugerida':''}</div>`;}
function install(){const card=$('coach');if(!card)return;const btn=document.createElement('button');btn.className='secondary';btn.textContent='Revisar mi semana';btn.addEventListener('click',review);card.after(btn);review();}
document.readyState==='loading'?document.addEventListener('DOMContentLoaded',install,{once:true}):install();})();
